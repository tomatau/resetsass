// require all entities and other use cases?

var Worker = {
    ('?client_id', '?project_id')
    seeNextTask: function(){
        // if there's no client id or project id
        //      find the task whose closing time is next
        //      if none have a closing time
        //          find task with earliest start time but not complete
        // find next task for client id
    },
    ('task_id')
    completeTask: function(){
        // get the task with the id
        // set it to complete
    }
}